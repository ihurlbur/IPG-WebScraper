<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ team.title() }} – Results</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900 p-6">
    <div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow-md">
        <h1 class="text-2xl font-bold mb-4">Results for {{ team.title() }} ({{ method.upper() }})</h1>

        {% if articles %}
            {% set sorted_articles = articles | sort(attribute='published', reverse=True) %}
            <ul class="space-y-4">
                {% for article in sorted_articles %}
                    <li class="border p-4 rounded">
                        <strong>{{ article.team.title() }} × {{ article.brand.title() }}</strong><br>
                        <a href="{{ article.link }}" class="text-blue-600 hover:underline" target="_blank">
                            {{ article.title }}
                        </a><br>
                        <span class="text-sm text-gray-600">{{ article.published }}</span>
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p class="text-gray-600">No matched articles found for {{ team.title() }}.</p>
        {% endif %}

        <div class="mt-6">
            <a href="{{ url_for('results_type') }}" class="text-blue-500 hover:underline">← Back to Results Type</a>
        </div>
    </div>
</body>
</html>

